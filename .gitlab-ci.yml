pages:
  image: simonics/emacs:latest  # Or use ubuntu:latest + apt-get install emacs
  script:
    - emacs -Q --script build-site.el
  artifacts:
    paths:
      - public
  only:
    - main  # Only build when pushing to main branch
